<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.guill.fhisa_admin">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:name=".Globals"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAam3CoohPwYSWGAJpfYDUC3KoGil5Sszg" />

        <activity
            android:name=".Antiguo.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".Opciones.DetallePosicionesCamion"
            android:parentActivityName=".MainActivity" />

        <service
            android:name=".NotificationJobScheduler"
            android:exported="false"
            android:permission="android.permission.BIND_JOB_SERVICE" />

        <activity
            android:name=".Preferences.CamionPreferences"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".Opciones.OpcionesCamionActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".Opciones.VelocidadActualActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".Preferences.CamionPreferencesActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".Opciones.AlbaranActivity" />
        <activity android:name=".Opciones.VehiculoActivity" />
        <activity android:name=".Opciones.ListaConsumosActivity" />
        <activity android:name=".Opciones.ListaAlbaranesActivity" />
        <activity android:name=".Opciones.MantenimientoActivity"></activity>

        <activity android:name=".OpcionesMenuActivity"
            android:parentActivityName=".MainActivity" />
    </application>

</manifest>